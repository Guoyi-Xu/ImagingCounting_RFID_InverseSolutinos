<?xml version="1.0"?> 
 <!-- NOTE: to generate java code for any extensions, configure the  -->
 <!-- properties file at $(propertiesFile) accordingly -->
 <!-- DO NOT EDIT THIS FILE -->
<project name="MyDesignTestDp" default="all" basedir=".">
     <property name="src" value="source"/> 
     <property name="build" value="target"/>
     <property name="classpath" value="${build}/classes"/>
     <property name="libdir" value="../libs/"/>

     <target name="init"> 
     	  <echo>using ant version '${ant.version}'</echo>
          <echo>using java version '${java.version}'</echo>
          <mkdir dir="${build}"/>
          <mkdir dir="${build}/classes"/>
     </target>


    <target name="src" depends="init">
        <echo message="Building src ..."/>
        <javac includeantruntime="false" 
            srcdir="${src}/example"
            destdir="${build}/classes"
            debug="true" 
            target="1.5"
            source="1.5">
            <classpath>
                <pathelement path="${classpath}"/>
                <fileset dir="${libdir}">
                    <include name="*.jar"/>
                </fileset>
            </classpath>
        </javac>
    </target>

    <target name="run-src" depends="src">
        <echo message="Running src ..."/>
            <java classname="example.MyDesignTestDp" fork="yes" >
            <arg value="${readername}"/>
            <classpath>
                <pathelement path="${classpath}"/>
                <pathelement location="target/classes"/>
                <fileset dir="${libdir}">
                    <include name="*.jar"/>
                </fileset>
            </classpath>
            </java>
    </target>

    <target name="all" depends="src">
    </target>

    <target name="run-all" depends="run-src">
    </target>
	<!-- Remove build directories -->
	<!-- ===================================== -->
	<target name="clean" >
		<delete dir="${build}" />
		<delete dir="${src}/${generated}" />
		<delete dir="${src}/org/llrp/ltkGenerator/generated" />
	</target>
	
</project>
